/** @format */

//import { useRoutes } from "react-router-dom";

import Main from "./admin";
import Other from "./other";

// export default function Routes() {
// 	return useRoutes([Main, ...Other]);
// }

const routes = [Main, ...Other];
export default routes;
